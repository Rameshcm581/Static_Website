# Base URL for the API layer (src/Api/apicall/axiosInstance.js)
VITE_API_BASE_URL=/api

# When true, contact + newsletter API calls resolve locally without a backend.
# Set to false once the backend endpoints exist.
VITE_MOCK_API=true
