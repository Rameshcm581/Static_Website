# Build-time env for the GitHub Pages deploy (.env is gitignored, this one is committed).
# Pages is static hosting — there is no backend, so the mock layer must stay on.
VITE_API_BASE_URL=/api
VITE_MOCK_API=true
